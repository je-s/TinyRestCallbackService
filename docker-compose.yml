services:
    service:
        build: ./
        image: tinyhttpcallbackservice:latest
        ports:
            - 5080
        volumes:
            - ${TINYHTTPCALLBACKSERVICE_DATA}:/TinyHttpCallbackService/data/
        restart: unless-stopped
    webhook-testservice:
        build: ./
        image: tinyhttpcallbackservice:latest
        ports:
            - 5081
        restart: unless-stopped
        entrypoint: "/usr/bin/python3 -u ./tools/webhook_testservice.py 5081"